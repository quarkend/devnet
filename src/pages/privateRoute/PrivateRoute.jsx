// import React from 'react'
// import { Redirect, Outlet } from 'react-router-dom'

// export default function PrivateRoute() {
//   const aut = null // determine if ownerized, from context or however you're doing it
//   // If ownerized, return an outlet that will render child elements
//   // If not, return element that will navigate to login page

//   return aut ? <Outlet /> : <Redirect to="/login" />
// }
